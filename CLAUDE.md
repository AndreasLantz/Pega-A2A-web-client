For reference when we are building this project, see ../Agent2Agent repo. It contains a fully working CLI tool that uses the A2A protocol for calling Pega agents and displaying the response in the CLI. It has auth to Pega set up, it has useful / commands and it is context aware by using same context id across messages.

